# Codex and GPT-5.6 evidence

## Codex contribution

This conversation is the primary Build Week implementation session. Codex first verified the
checkout, remote, baseline, public architecture, registered fixture, exact/QUBO/Ising routes,
Qiskit route, learning console, tests, and safety scanner. It then implemented, tested, reviewed,
published, and merged two deliberately bounded PRs:

- PR #16, deterministic core: head `f0931a3`, merge `39489b0`.
- PR #17, Guided Experiment: head `e3cc84a`, merge `06f5531`.

Codex accelerated contract design, adapters, renderer composition, fake-client tests, CI diagnosis,
runtime measurement, AppTest audit, release documentation, and clean-snapshot verification.

## Human decisions

Gwri selected the fixed synthetic fixture, Education category, exact-first authority, three learner
levels, prominent Guided Experiment hierarchy, p=1/256-shot quick-run boundary, optional
`gpt-5.6-sol` feature, privacy boundary, staged merge workflow, and omission of musical-preview P2
when it could distract from core reliability.

## GPT-5.6 runtime boundary

GPT-5.6 Sol optionally rewrites a validated result for the selected learner level. It receives no
arbitrary prompt, repository file, secret, environment dump, or hidden evidence. A strict response
schema and post-response checks enforce grounded fields, known numeric values, exact-result
consistency, and prohibited-claim policy. Scientific values always come from deterministic code.
The deterministic explanation is the complete fallback.

## Primary-session feedback

`PENDING USER ACTION — invoke /feedback in this primary Codex session and save the returned session ID.`

No identifier has been fabricated.
