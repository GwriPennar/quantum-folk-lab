# Architecture

Preprocessing is classical: synthetic sequence generation, interval extraction, similarity scoring, and graph construction all use ordinary deterministic algorithms. Quantum methods are tested only on the bounded global optimisation kernel represented by a QUBO.
