# EXP-009A — real-data quantum truth gate

EXP-009A froze a four-family × two-setting objective and strict coarse-feature parser before reading
the selected external notation. The external cache, revision, files, selected records, raw-record
hashes, normalized fingerprints, and deterministic row choices passed verification.

Feature extraction then stopped on `bold-deserter-longdance` (O'Neill 1791) because the record
contains an inline key change. `FEATURE-CONTRACT.md` explicitly rejects inline key changes rather
than silently simplifying them. The rule was not weakened after observing the data.

Consequently no derived-feature, pairwise-distance, QUBO, Ising, exact-result, or local-QAOA result
file was generated. Exact enumeration and local QAOA were not run. This is the intended fail-closed
behavior of the scientific truth gate.

The synthetic Build Week fixture remains the default judge-runnable route. No raw notation is
redistributed. No IBM service or hardware was accessed.
