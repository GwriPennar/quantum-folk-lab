# EXP-009A-R1 — inline-key successor truth gate

EXP-009A-R1 separately froze and proved a general policy that removes well-formed inline `K:`
metadata before note tokenisation. The historical EXP-009A NO-GO remains unchanged.

The pinned IrishMAN revision, split hashes, all eight canonical record hashes, normalized
fingerprints, and deterministic row tie-breaks were verified again. The successor then failed
closed because selected records contain additional inline metadata classes (`M:` and `Q:`) for
which the frozen contract defines no removal policy.

No complete successor feature table, pairwise distances, QUBO, Ising result, exact optimum, QAOA
protocol, QAOA result, or hardware protocol was generated. Raw notation and inline-field content
were not printed, logged, exported, or committed.

Outcome: `NO-GO — SUCCESSOR PARSER OR OBJECTIVE INVALID`.
