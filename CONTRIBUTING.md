# Contributing

Keep examples synthetic unless a future public-data adapter has documented licence and provenance review. Run `python scripts/check_public_safety.py`, `ruff check .`, `ruff format --check .`, `mypy`, and `pytest` before proposing changes.
